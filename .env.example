# Do NOT commit .env (it's gitignored). Copy this to .env and fill in values.
IMAP_HOST=
IMAP_USER=
IMAP_PASSWORD=
# Optional
IMAP_PORT=993
IMAP_SSL=true
IMAP_MAILBOX=INBOX
# Examples: codex, [support]
IMAP_SUBJECT_CONTAINS=codex
IMAP_ALL_MAILBOXES=false
